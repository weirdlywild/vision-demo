# ===================================
# DIY Repair Diagnosis API - Environment Configuration
# ===================================

# OpenAI Configuration (Required)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# OpenAI Settings
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=2000
OPENAI_TEMPERATURE=0.3

# Image Processing
MAX_IMAGE_SIZE_MB=10
MIN_IMAGE_DIMENSION=100
MAX_IMAGE_DIMENSION=4096
RESIZE_MAX_DIMENSION=1024
BLUR_THRESHOLD=30
MIN_BRIGHTNESS=40
MAX_BRIGHTNESS=240

# Caching
CACHE_TTL_SECONDS=3600
PERCEPTUAL_CACHE_TTL_SECONDS=86400
MAX_CACHE_ENTRIES=1000

# Session Management
SESSION_TTL_MINUTES=30
MAX_SESSION_HISTORY=3
CLEANUP_INTERVAL_MINUTES=10

# API Configuration
API_TIMEOUT_SECONDS=30
MAX_RETRIES=3
BACKOFF_FACTOR=2

# Performance
UVICORN_WORKERS=2
UVICORN_TIMEOUT=60

# Security (IMPORTANT: Change these for production!)
ALLOWED_ORIGINS=*
API_PASSWORD=your-secure-password-here
RATE_LIMIT_PER_MINUTE=10

# ===================================
# Railway Deployment Notes:
# ===================================
# 1. Set OPENAI_API_KEY in Railway environment variables
# 2. Set a strong API_PASSWORD (this protects your API from unauthorized access)
# 3. PORT is automatically set by Railway - do not set it manually
# 4. ALLOWED_ORIGINS=* allows all domains (fine for most cases)
# 5. All other settings have sensible defaults
#
# See RAILWAY_DEPLOYMENT.md for complete deployment guide
